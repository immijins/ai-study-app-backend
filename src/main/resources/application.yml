spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST}:6543/postgres?sslmode=require&prepareThreshold=0
    username: postgres.sbdjmyxhtdidwdsicnqj
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

openai:
  api-key: ${OPENAI_API_KEY}
  model: gpt-4o-mini